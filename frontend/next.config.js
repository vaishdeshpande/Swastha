/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed for Vercel.
  // LiveKit WebRTC works through the browser — no server-side WebSocket needed.
};

module.exports = nextConfig;
